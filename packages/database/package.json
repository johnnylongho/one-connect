{
  "name": "@one-connect/database",
  "version": "0.1.0",
  "main": "index.ts",
  "types": "index.ts",
  "scripts": {
    "lint": "eslint .",
    "generate": "prisma generate",
    "db:push": "prisma db push",
    "db:seed": "prisma db seed"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0"
  },
  "devDependencies": {
    "prisma": "^5.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.0.0",
    "@repo/typescript-config": "workspace:*",
    "@repo/eslint-config": "workspace:*"
  },
  "prisma": {
    "seed": "tsx prisma/seed.ts"
  }
}
